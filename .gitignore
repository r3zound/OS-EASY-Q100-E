# 临时文件
*.tmp
*.bak
*.orig
*.log

# 编辑器
.vscode/
.idea/
*.swp
*~
.DS_Store

# 大二进制文件（避免推送到 GitHub）
# 注意：BIOS .bin 文件本来应该跟踪，但 32MB 单文件可以适当 ignore
# 实际策略：把 bin 单独放 backups/ 但仍然跟踪，便于别人下载
# 如果觉得太大，启用下面这行：
# backups/*.bin

# Windows
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# 编译产物
build/
dist/
node_modules/
__pycache__/
*.pyc

# 工具临时输出
uefitool_temp/
mmtool_temp/
